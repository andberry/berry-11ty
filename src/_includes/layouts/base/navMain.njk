<nav id="main-nav" uk-navbar class="c-nav-main">
    <ul>
        {% for item in navMain %}
        <li class="">
            <a href="{{ item.url }}" class="{{ item.class }}">{{ item.text }}</a>
        </li>
        {% endfor %}
    </ul>
</nav>